# Hermes Roofing Company — website

Static site for Hermes Roofing Company (Austin, TX), built for Git + Vercel with live Google reviews.

## Deploy (5 minutes)

1. Push this folder to a Git repository (GitHub/GitLab/Bitbucket).
2. In Vercel: **Add New Project → Import** the repo. No framework preset or build command needed — it's a static site, and Vercel automatically serves the `api/` folder as serverless functions.
3. Deploy. Done — the site works immediately, with reviews in "minimal" mode until you complete the Google setup below.

`vercel.json` enables clean URLs (`/services` instead of `/services.html`).

## Live Google reviews setup

The homepage reviews section pulls live data from the business's Google Business Profile through `api/reviews.js`. To enable it:

1. **Get the Place ID.** Go to Google's Place ID Finder (search "Google Place ID finder"), search "Hermes Renovations Inc Austin TX" (or the renamed profile), and copy the Place ID (starts with `ChIJ...`).
2. **Create an API key.** In Google Cloud Console: create a project → enable **Places API** → create an API key under Credentials. Restrict the key to the Places API. Billing must be enabled (Google's free monthly credit comfortably covers this site's cached usage).
3. **Add environment variables** in Vercel → Project → Settings → Environment Variables:
   - `GOOGLE_PLACES_API_KEY` = your key
   - `GOOGLE_PLACE_ID` = the Place ID
4. Redeploy.

Notes:
- Google returns a **maximum of 5 reviews** per request — the layout uses 1 featured + up to 4 cards, so that's a perfect fit. Reviews are cached at the edge for 6 hours, keeping API costs near zero.
- The front end automatically features a review mentioning the metal-detector cleanup if one exists, then prefers reviews naming crew members (Peter, Landon, Jesse, Corey), and highlights their names in green.

## Fallback reviews (before the API is configured)

`data/reviews-fallback.json` can show hand-picked reviews when the API isn't set up. **It ships locked**: nothing renders until you set `"approved": true` AND mark every entry `"verbatim": true`.

Only do that after replacing every placeholder with **word-for-word reviews copied from the company's real Google/Nextdoor profiles**, with real reviewer first names. Do not edit, improve, or invent review text — publishing fabricated or altered testimonials violates FTC rules and Google's policies.

## Contact form

The form on `/contact` posts to Formspree. Create a free form at formspree.io and replace `YOUR_FORM_ID` in `contact.html` with your form's ID. Until then, the phone and email links work regardless.

## Swapping in real photos

All images live in `/images` with SEO-descriptive filenames (service + location). To swap a photo, replace the file but **keep the filename**, or use a new descriptive name in the same pattern (`service-detail-city-tx.jpg`) and update the reference. Every `<img>` has matching alt text — update it if the subject changes. Add new gallery photos in `gallery.html` (and optionally the homepage grid).

Crew photos: the crew cards currently use green initial circles. When headshots are ready, replace each `<div class="crew-avatar">XX</div>` with `<img class="crew-avatar" src="/images/crew-name-hermes-roofing-austin.jpg" alt="Name, role">` (add `object-fit: cover` styling is already handled by the class dimensions).

## Editing content

- Site-wide phone/address/hours: appear in each page's header/footer (search for `522-7310`).
- Homepage sections: `index.html`.
- Secondary pages were generated by `build_pages.py` — you can edit the HTML files directly, or edit the script and re-run `python3 build_pages.py` to regenerate them all consistently.
- City pages live in `/service-areas/`. Strengthen each over time with real local photos and project notes — genuinely local content is what ranks.

## Structure

```
index.html              Homepage (hero, areas, reviews, crew, gallery, FAQ, storm, guarantee)
services.html           All services with anchor links
storm-insurance.html    Storm/hail + insurance claims landing page (point ads here)
our-crew.html           About + crew + how-we-work
gallery.html            Full photo gallery
contact.html            Contact info + form
service-areas/*.html    7 city landing pages with local schema
api/reviews.js          Serverless function → Google Places API
data/reviews-fallback.json  Locked fallback reviews
css/styles.css          Design system (Heritage Craft: cream + forest green)
js/main.js              Nav, reviews rendering
sitemap.xml, robots.txt, favicon.svg, vercel.json
```

## Pre-launch checklist

- [ ] Set `GOOGLE_PLACES_API_KEY` and `GOOGLE_PLACE_ID` in Vercel (or fill + unlock the fallback file with verbatim reviews)
- [ ] Replace `YOUR_FORM_ID` in contact.html with a real Formspree ID
- [ ] Update the canonical domain in pages + sitemap if not using hermesroofing.com
- [ ] Confirm the phone number (512-522-7310) and address are the canonical NAP everywhere
- [ ] Point Google Business Profile's website field at the new domain
